version: 2
updates:
  - package-ecosystem: "terraform"
    directory: "/"
    schedule:
      interval: "daily"
    # Ignore Terraform providers only (not Git modules)
    ignore:
      - dependency-name: "hashicorp/*"
      - dependency-name: "azurerm"
      - dependency-name: "azure/*"
